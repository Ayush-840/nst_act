let users=[]; // Add all the students with average value >35 in this array.


function filterStudents(studentList) {
    studentList.filter((student)=>{
        let sum=student.mark.reduce((acc,mark)=>acc + mark, 0)
        let avg=sum/
    })

    
   
}