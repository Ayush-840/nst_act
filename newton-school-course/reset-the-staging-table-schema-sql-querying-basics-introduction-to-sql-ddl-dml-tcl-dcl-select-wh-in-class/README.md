# Reset the staging table - Schema, SQL Querying Basics, Introduction to SQL, DDL, DML, TCL, DCL, SELECT, WH ... - In Class

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `jf0xk3avybfn`  
**Submission Time:** 2026-08-20T20:00:14.890Z  

## Problem Statement

The staging_orders table (order_id, customer, amount) holds imported rows that must all be removed using a single DDL command that empties the table but keeps its structure (not a row-by-row DELETE). After emptying it, return every row of staging_orders (order_id, customer, amount).

The staging_orders table initially contains:

+----------+----------+--------+
| order_id | customer | amount |
+----------+----------+--------+
| 1 | Aarav | 500 |
| 2 | Priya | 750 |
+----------+----------+--------+

### Output
TRUNCATE TABLE
 order_id | customer | amount 
----------+----------+--------
(0 rows)

## Solution

```js
// ─── 1 ───
Truncate table staging_orders;

select order_id, customer, amount from staging_orders;

// ─── 2 ───
TRUNCATE TABLE
 order_id | customer | amount 
----------+----------+--------
(0 rows)
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
