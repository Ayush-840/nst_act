# Today's Income - JS Practice Questions

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `my9j8kk5qexh`  
**Submission Time:** 2026-08-14T05:02:39.313Z  

## Problem Statement

Mr. Cashier at JSElectronics needs help calculating the total income for the day.

Implement the function:

function getTotalIncomeGenerated(orders)

Process:
For each order:

 * Check stock using checkQuantity(name).

 * If available:
 
 * Add the price to income.

 * Reduce stock by 1 using updateQuantity(name).

 
 

 * If out of stock, skip the order.

Pre-defined Functions:

 * checkQuantity(name): accepts order name as a parameter and returns the available stock quantity.

 * updateQuantity(name): accepts order name as a parameter and reduces the available stock quantity by 1.

Function getTotalIncomeGenerated Parameter:

 * 
 orders: Array of objects {name: string, price: number}
 

Function getTotalIncomeGenerated return:

 * Return the total income from successful transactions.

### Input
Input shall contain a single-line JSON string of an object with 2 keys ("orders", "stocks") with the value of an array of objects.

* orders: an array of objects, where each object has 2 keys name (string) & price (number).

* stocks: an array of objects, where each object has 2 keys name (string) & quantity (number).

### Output
total income generated from all successful transactions.

Note: You don't need to log anything in the console. output logs will be handled from the backend. you just need to return the final income.

### Example
Example
Input:
{"orders":[{"name":"Laptop","price":50000},{"name":"Phone","price":30000 }],"stocks":[{"name":"Laptop","quantity":5},{"name":"Tablet","quantity":3}]}

Output:
50000

## Solution

```js
function getTotalIncomeGenerated(orders){
    let price=0
    orders.forEach(())

}
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
