# The Canteen Item-Wise Analytics API

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `9r05njtpsggd`  
**Submission Time:** 2026-09-14T17:18:42.656Z  

## Problem Statement

You are building a simple analytics service for a college canteen.

The canteen stores all food orders in a text file named orders.txt, where each line represents one order.

orders.txt

Burger
Pizza
Burger
Burger
Pizza
Task:

Enhance the existing canteen analytics service to generate detailed order statistics.

The server should read the file and return:

 * Total number of orders

 * Number of orders for each food item

Expected Response:

{
 "totalOrders": 5,
 "burgerOrders": 3,
 "pizzaOrders": 2
}
Rules:

 * The key format is: &lt;lowercase item name&gt;Orders (e.g. "Burger" &rarr; "burgerOrders")

 * The response should dynamically include a key for every unique item found in orders.txt.(not just Burger and Pizza)

 * Empty lines should be ignored

## Solution

```js
// BUG: Fix the lines below so the server
    // returns a proper JSON response
    res.end(JSON.stringify(user));
});
// DO NOT MODIFY BELOW THIS LINE
if (require.main === module) {
    app.listen(3000, () => {
        console.log('Server running on http://localhost:3000');
    });
}
module.exports = { app };
    };
        name: 'John'
    const user = {
const app = http.createServer((req, res) => {
const http = require('http');
    res.writeHead(200,{'Content-Type': 'application/json'})
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
