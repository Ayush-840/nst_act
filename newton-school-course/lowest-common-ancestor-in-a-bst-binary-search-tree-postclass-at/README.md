# Lowest Common Ancestor in a BST - Binary Search Tree - Postclass - AT

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `1byu70nnj82b`  
**Submission Time:** 2026-08-20T17:45:06.980Z  

## Problem Statement

Given a Binary Search Tree and two integers n1 and n2, your task is to find the lowest common ancestor (LCA) of the two given integers.

The lowest common ancestor is defined between two nodes p and q as the lowest node in T that has both p and q as descendants (where we allow a node to be a descendant of itself).

Note: Values n1 and n2 may or may not be present.

### Input
Your Task
Since this is a functional problem, you do not need to handle input or output. Your task is to complete the function LCA(), which takes the root, n1 and n2 as the input parameter.

Custom Input
The first line of input contains an integer, N, representing the number of nodes in the tree.
The next line consists of space-separated integers denoting the level order traversal of the tree, where non-negative values represent node values, and -1 indicates a null node.
The next line of input contains n1 and n2 representing the node values.
Note: Once a node is marked as -1, no further information about its children is provided.

### Output
Return the node containing LCA of n1 and n2 if both values exist otherwise None. 

Custom Output
The driver code will print the data if both n1 and n2 exist. For None, the driver code will print -1.

### Example
Input:
6
5 4 6 3 -1 -1 7 -1 -1 -1 8
7 8
Output:
7
Explanation:

Here the LCA of 7 and 8 is 7.

Input:
3
5 3 6
3 6
Output:
5
Explanation:

Here the LCA of 3 and 6 is 5.

## Solution

```py
'''
class Node:
    def __init__(self, val, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right 
'''    
def LCA(node, n1, n2):
    root = node
    ans = None
    while root:
        if root.val > max(n1, n2):
            root = root.left 
        elif root.val < min(n1, n2):
            root = root.right 
        else:
            ans = root 
            break 

    x = 0  
    def f(root):
        nonlocal x
        if not root:
            return
        f(root.left)
        x += (root.val == n1 or root.val == n2)
        f(root.right)
    f(node)

    return ans if x == 2 else None
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
