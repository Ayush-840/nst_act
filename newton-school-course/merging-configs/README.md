# Merging configs

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `0rqonl9am3zc`  
**Submission Time:** 2026-09-01T20:39:56.865Z  

## Problem Statement

Context : 

You are working on a project where multiple team members are maintaining separate configuration files for different environments (e.g., dev, test, prod). These configuration files are stored in a directory named configs. The files are named dev_config.txt, test_config.txt, and prod_config.txt.

Task : 

Your task is to combine the contents of these files into a single file called merged_config.txt, but the contents should be in the order of dev, test, and then prod. Additionally, ensure that the output file includes a header line before each section, indicating which environment the configurations belong to.

The final merged_config.txt should contain the content in this format: 

# Dev Configuration

[ dev-config.txt content here ]

# Test Configuration

[ test.config.txt content here ]

# Prod Configuration

[ prod_config.txt content here ]

Write the command(s) you would use to achieve this.

### Input
NA

### Output
NA

### Example
NA

## Solution

```js
# echo "# Dev Configuration" > merged_config.txt
# cat configs/dev_config.txt >> merged_config.txt

# echo "" >> merged_config.txt
# echo "# Test Configuration" >> merged_config.txt
# cat configs/test_config.txt >> merged_config.txt

# echo "" >> merged_config.txt
# echo "# Prod Configuration" >> merged_config.txt
# cat configs/prod_config.txt >> merged_config.txt



echo "# Dev Configuration" >> merged_config.txt
cat configs/dev_config.txt >> merged_config.txt
echo "" >> merged_config.txt

echo "# Test Configuration" >> merged_config.txt
cat configs/test_config.txt >> merged_config.txt
echo "" >> merged_config.txt

echo "# Prod Configuration" >> merged_config.txt
cat configs/prod_config.txt >> merged_config.txt



# cat configs/dev_config.txt configs/test_config.txt configs/prod_config.txt >> merged_config.txt
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
