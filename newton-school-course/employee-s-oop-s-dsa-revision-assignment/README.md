# Employee's OOP's - DSA Revision Assignment

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `ao0k7lq9yp2z`  
**Submission Time:** 2026-08-15T07:32:18.644Z  

## Problem Statement

You must implement an Employee class that models basic employee information and computes annual salary. The class will be tested automatically; you only need to define the class and its method.

Attributes

Your Employee class must include:

 
 name &mdash; string representing the employee&rsquo;s name

 

 
 salary &mdash; integer representing the employee&rsquo;s monthly salary

 

Methods

Implement the following:

 annual_salary() &rarr; returns the employee&rsquo;s annual salary, computed as &lt;inlineMath&gt;\text{salary} \times 12&lt;/inlineMath&gt;

Task

Read the employee&rsquo;s name and monthly salary from input.

Create an Employee object using the constructor.

Call the annual_salary() method on the created object.

Print the returned value.

### Input
The first line contains the employee’s name (e.g., Rahul).
The second line contains the monthly salary (e.g., 20000).

### Output
Print a single integer: the employee’s annual salary using the annual_salary method.

### Example
Input
Rahul
20000

Output
240000

Input
Sai
1000000

Output
12000000

## Solution

```py
class Employee:
    # Constructor to initialize name and salary
    def __init__(self, name: str, salary: int):
        self.name = name
        self.salary = salary

    # Method to calculate and return the annual salary
    def annual_salary(self):
        return self.salary * 12

# Take input from user
name_input = input().strip()
salary_input = int(input().strip())

# Create an Employee object
emp = Employee(name_input, salary_input)

# Print the annual salary
print(emp.annual_salary())
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
