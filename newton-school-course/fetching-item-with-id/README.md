# Fetching item with id

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `95lml0560c4z`  
**Submission Time:** 2026-09-14T18:27:36.095Z  

## Problem Statement

Create a Node.js server which:

 
 If GET / is called, responds with &ldquo;Home page&rdquo;

 

 
 If GET /item/:id (for example /item/123) is called, extract the id from the URL manually and respond with &ldquo;Fetching item with id 123&rdquo;

 

 
 If the route is not recognized, respond with a 404 status code and the text "404 Not Found".

## Solution

```js
});
server.listen(3000, () => {
  console.log('Server listening on port 3000');
  if(req.method==="GET" && req.url==="/"){
    res.writeHead(200,{"content-Type":"text/plain"});
  }
    res.end("Home page");
  // Your code here
  }
    res.writeHead(404,{"content-type":"text/plain"});
  }
    res.end("404 Not Found");
    const id= req.url.split("/")[2];
    res.writeHead(200,{"content-type":"text/plain"})
    res.end(`Fetching item with id ${id}`)
});
const server = http.createServer((req, res) => {
  else if(req.method==="GET" && req.url.startsWith("/item/")){
  else
  {
module.exports = { server };
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
