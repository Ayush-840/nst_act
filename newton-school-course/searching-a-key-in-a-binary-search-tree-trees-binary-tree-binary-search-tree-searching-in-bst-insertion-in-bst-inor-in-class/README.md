# Searching a key in a Binary Search Tree - Trees, Binary Tree, Binary Search Tree, Searching in BST, Insertion in BST, Inor ... - In Class

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `qe9he6u9z2qy`  
**Submission Time:** 2026-08-17T05:45:11.759Z  

## Problem Statement

You are given the root of a binary search tree (BST) and a value key. Search if the value key exist in the tree. Return 1 if it exist otherwise 0.

Note: A Binary Search Tree is a Binary Tree where every node&#39;s left child has a lower value, and every node&#39;s right child has a higher value.

### Input
User Task
Since this is a functional problem, you do not need to handle input or output. Your task is to complete the function search_in_bst(), which takes the root node and the key as its parameters.

Custom Input
The first line of input contains two space separated integers, N and key, representing the number of nodes in the tree and the key value respectively.
The next line consists of space-separated integers denoting the level order traversal of the tree, where non-negative values represent node values, and -1 indicates a null node.
Note: Once a node is marked as -1, no further information about its children is provided.

### Output
Return 1 if the key exists in the given binary tree, else return 0.

### Example
Input
9 60
50 25 75 12 30 60 85 -1 -1 -1 -1 52 70
Output
1
Explanation
The following image depicts the searching process in the BST:

## Solution

```py
'''
class Node:
    def __init__(self, val=0):
        self.val = val
        self.left = None
        self.right = None
'''

def search_in_bst(root, key):
    if root==None:
        return 0
    if root.val==key:
        return 1
    if key < root.val:
        return search_in_bst()
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
