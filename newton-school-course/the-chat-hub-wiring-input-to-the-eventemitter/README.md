# The Chat Hub: Wiring Input to the EventEmitter

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `ey9vvwt94vcc`  
**Submission Time:** 2026-09-07T03:53:00.560Z  

## Problem Statement

In Chunk 1, you created a chatSystem EventEmitter and registered a listener for the messageReceived event.

In Chunk 2, you collected the sender&#39;s name and message using nested rl.question() calls.Now, connect these two parts.

Task:-

Inside the second rl.question() callback:

 * Replace the placeholder with a chatSystem.emit() call.

 * Emit the messageReceived event.

 Pass an object containing:

 username: the sender&#39;s name (currentName)

 text: the message entered by the user (messageInput)

When the event is emitted successfully, the listener from Chunk 1 should automatically display the notification in the terminal.

Test Cases:-

 * source code should contain a chatSystem.emit() call

 * source code should pass a username property inside the emitted payload

 * source code should pass a text property inside the emitted payload

 * source code should emit the event inside the nested rl.question() callback

 * listener should correctly handle the emitted data payload

## Solution

```js
// 2. The Input Interface
const rl = readline.createInterface({
    input: process.stdin,
    output: process.stdout
});
// 3. Ask questions and Emit
rl.question('Enter sender name: ', (nameInput) => {
    const currentName = nameInput.trim() || 'Anonymous';
    rl.question(`Type message for ${currentName}: `, (messageInput) => {
        // TODO: Emit the 'messageReceived' event here!
        //       Pass an object containing:
        //       - username: currentName
        //       - text: messageInput
        rl.close();
    });
});
// DO NOT MODIFY BELOW THIS LINE
module.exports = { chatSystem, rl };
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
