# Check Eligibility for Marriage in India Using Promises - JS Practice Questions

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `j2emti87clq2`  
**Submission Time:** 2026-08-13T05:34:50.342Z  

## Problem Statement

You are tasked with creating a function called checkMarriageEligibility that determines whether a person is eligible for marriage in India based on their age and gender.

Eligibility Criteria (India):

1. Men: Legal marriage age is 21 years or older.

2. Women: Legal marriage age is 18 years or older.

Function Requirements:

 
 Create a function checkMarriageEligibility(age, gender) that:

 - Accepts two parameters: age (number) and gender (string - "male" or "female").

 - Returns a promise that checks eligibility based on age and gender.

 

 
 Promise Behavior:

 If a man ("male") is 21 or older, the promise resolves with:

 &lt;code&gt;"You are eligible for marriage in India."&lt;/code&gt;

 If a man ("male") is below 21, the promise rejects with:

 &lt;code&gt;"You are not eligible for marriage in India."&lt;/code&gt;

 If a woman ("female") is 18 or older, the promise resolves with:

 &lt;code&gt;"You are eligible for marriage in India."&lt;/code&gt;

 

 * 
 If a woman ("female") is below 18, the promise rejects with:

 &lt;code&gt;"You are not eligible for marriage in India."&lt;/code&gt;

 

 
 Promise Handling:

 
 Use .then() to handle the resolved promise and print the success message.

 Use .catch() to handle the rejected promise and print the failure message.

 
 

### Input
The first line of input consists of age and gender with space separated
age (number): The age of the person. 
gender (string): The gender of the person (either 'male' or 'female').
Note: age ranges from 1 year to 100 years only

### Output
The function checkMarriageEligibility returns a promise that:
Resolves with the message "You are eligible for marriage in India." 
if:
For men, the age is 21 or older.
For women, the age is 18 or older.

Rejects with the message "You are not eligible for marriage in India." 
if:
For men, the age is below 21.
For women, the age is below 18.

### Example
Example-1
Input: 
25 male
Output: 
You are eligible for marriage in India.

Example-2
Input: 
17 female
Output: 
You are not eligible for marriage in India.

Example-3
Input: 
20 male
Output: 
You are not eligible for marriage in India.

Example-4
Input: 
19 female
Output: 
You are eligible for marriage in India.

## Solution

```js
function mainFunction(age ,gender){
    if()

}

// write your code here
function checkMarriageEligibility(age,gender){
    return new Promise((res,rej)=>{
        if(gender==="male" && age>=21){
            res("You are eligible for marriage in India.")
        }if(gender==="fenale" && age)
    })

}
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
