# Divisible and Range - DSA Revision Assignment

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `c9jzopbfpzil`  
**Submission Time:** 2026-08-16T10:56:40.011Z  

## Problem Statement

You are given an integer array &lt;inlineMath&gt; arr &lt;/inlineMath&gt; consisting of &lt;inlineMath&gt; n &lt;/inlineMath&gt; elements, and the three integers &lt;inlineMath&gt; k, l, &lt;/inlineMath&gt; and &lt;inlineMath&gt; r &lt;/inlineMath&gt;.

Your task is to find all the elements in &lt;inlineMath&gt; arr &lt;/inlineMath&gt; that are divisible by &lt;inlineMath&gt; k &lt;/inlineMath&gt; and lie within the inclusive range &lt;inlineMath&gt; [l, r] &lt;/inlineMath&gt; and then print their sum.

### Input
The first line contains an integer n - the size of the array.

The next line contains n separated integers a1, a2, a3, .... ., an - the elements of the array. 

The third line contains three space-separated integers — k, l, and r.

Constraints
1 i Input
5
2 6 9 12 15
3 5 15

Output
42

Explanation
Elements divisible by 3 and in the range [5, 15] are: 6, 9, 12, and 15.
Sum = 6+9+12+15=42

## Solution

```js
# Your code here
# Read input values
n = int(input())
arr = list(map(int, input().split()))
k, l, r = map(int, input().split())

# Filter elements that are divisible by k and lie within the range [l, r]
total_sum = sum(x for x in arr if x % k == 0 and l <= x <= r)

# Print the result
print(total_sum)
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
