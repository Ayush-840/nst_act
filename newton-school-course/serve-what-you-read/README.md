# Serve What You Read

## Course Context
**Course:** Newton School Course  
**Problem Slug:** `lg26w1s5vslf`  
**Submission Time:** 2026-09-14T16:59:20.575Z  

## Problem Statement

You are given a pre-created Node.js web server that listens on port 3000.

The server should read the contents of a file named message.txt and return it as the HTTP response.

Instead of sending a hardcoded response, the server should read the contents of a file named message.txt and return the file contents as the HTTP response.

If the file is successfully read:

 * Respond with status code 200

 * Send the file contents as the response body

If the file cannot be read:

 * Respond with status code 500

 * Send the message: "Internal Server Error: Could not read file."

message.txt file might contain:

Hello from the file!

## Solution

```js
res.end(data);
        }
        else{
            res.writeHead(200,{"content-Type":"text/plain"});
            })
            res.end("Internal Server Error: Could not read file.")
        }
        if (err){
            res.writeHead(500,{"content-Type":"text/plain"
    // TODO: Read message.txt asynchronously using fs.readFile()
    fs.readFile(filePath,(err,data)=>{
    const filePath = path.join(__dirname, 'message.txt');
const app = http.createServer((req, res) => {
    })
    // TODO: If file read is successful:
    //       - respond with status 200
```

---
*Auto-generated by [UniSync Chrome Extension](https://github.com).*
